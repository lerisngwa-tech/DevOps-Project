replicaCount: 1

ingress:
  # Empty = catch-all rule, matches any Host header (including the raw ALB DNS
  # name). Set a real domain here once you own one and point it at the ALB.
  host: ""

resources:
  requests:
    cpu: 50m
    memory: 96Mi
  limits:
    cpu: 250m
    memory: 192Mi
